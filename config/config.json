{
  "_comment_baseUrl": [
    "baseUrl is NOT read from here — it MUST be set via the BASE_URL environment",
    "variable in your .env file. See playwright.config.ts for details."
  ],
  "timeouts": {
    "navigation": 30000,
    "element": 10000
  },

  "_comment_execution": [
    "Execution visibility — controls whether the browser shows on screen:",
    "  \"headless\": false  →  Visible (Headed) mode — watch tests run in real time",
    "  \"headless\": true   →  Background (Headless) mode — runs silently, no UI"
  ],
  "headless": true,

  "_comment_video": [
    "Video recording — records a video of each test run.",
    "  mode — when to record/keep videos:",
    "    \"on\"                  → record and keep a video for EVERY test",
    "    \"off\"                 → do not record video",
    "    \"retain-on-failure\"   → record for every run, keep only failures",
    "    \"on-first-retry\"      → record only on the first retry of a test",
    "  quality — resolution of the recorded video (higher = better quality):",
    "    \"low\"      → 800x450",
    "    \"medium\"   → 1280x720",
    "    \"high\"     → 1366x768 (matches the test viewport — sharpest)",
    "Both settings can be overridden via the VIDEO_MODE / VIDEO_QUALITY",
    "environment variables (e.g. in .env) — they take precedence over config.json."
  ],
  "video": {
    "mode": "on",
    "quality": "high"
  },

  "_comment_trace": [
    "Trace recording — records a detailed execution trace (actions, network requests,",
    "DOM snapshots) that can be inspected in the Playwright Trace Viewer",
    "(npx playwright show-trace <file.zip>).",
    "  mode — when to collect/keep traces:",
    "    \"on-first-retry\"      → collect a trace when retrying a failed test (default)",
    "    \"on\"                  → collect a trace for EVERY test",
    "    \"off\"                 → do not collect traces",
    "    \"retain-on-failure\"   → collect for every run, keep only failures",
    "Can be overridden via the TRACE_MODE environment variable (takes precedence)."
  ],
  "trace": {
    "mode": "on-first-retry"
  },

  "saudiPhone": {
    "prefixes": ["055", "056", "057", "058", "059", "053", "054", "050"],
    "landlinePrefixes": ["011", "012", "013", "014", "016", "017"],
    "mobileFormat": "+9665#########"
  },
  "_comment_staff": "Staff names for patient Select2 interactions. Also available in config/patient-scenarios/*.scenario.json (_config.staff) for per-scenario overrides.",
  "staff": {
    "primaryTeamLeaderNurse": "Test Nurse",
    "primaryNurseName": "Test Nurse",
    "primaryPhysicianName": "Test_Doctor",
    "primarySocialWorkerName": "waad.albaqami"
  },
  "locale": "ar-SA",
  "timezone": "Asia/Riyadh",

  "appointment": {
    "_comment_targetPatientIdentifier": [
      "targetPatientIdentifier — the Patient ID searched on the Patients list page",
      "before creating/viewing appointments. This is the SINGLE source of truth used",
      "by every appointment test — do NOT duplicate it in scenario files.",
      "Other available IDs in staging: [109, 120, 122, 107, 176, 177, 144, 158]"
    ],
    "targetPatientIdentifier": "222",

    "_comment_visitType": [
      "visitType — the appointment/visit type used by EVERY appointment test.",
      "This is the SINGLE source of truth (like targetPatientIdentifier) — do NOT",
      "duplicate it in scenario files. Must be 'Treatment Nurse Visit' for the",
      "Flow Sheet form to apply (see tests/flow-sheet-from-appointment.spec.ts)."
    ],
    "visitType": "Treatment Nurse Visit"
  },

  "_comment_visitId": [
    "visitId — the target visit ID located in the Visits directory (list page /visits)",
    "whose visit-form tabs (Flow Sheet, Patient Assessment, Discontinue Of Hemodialysis,",
    "Vascular Access Assessment, Respiratory Triage, Referrals) are filled out by the",
    "visit-form tests (tests/flow-sheet.spec.ts, tests/patient-assessment.spec.ts,",
    "tests/discontinuation.spec.ts, tests/vascular-access.spec.ts,",
    "tests/respiratory-triage.spec.ts, tests/referral.spec.ts). Each test/script finds",
    "the row in the Visits list and opens the corresponding form/tab for this visit.",
    "This is the SINGLE source of truth shared by ALL visit-form tests and scripts —",
    "do NOT duplicate the visit ID in scenario files.",
    "The visit must be of type 'Treatment Nurse Visit' for the forms to apply."
  ],
  "visitId": "1006",

  "_comment_appointmentConfig": "Appointment test parameters (defaultDurationMinutes, etc.) live in the _config block of each file in config/appointment-scenarios/, and form fields (date, time, notes, etc.) in _fields. targetPatientIdentifier, visitType and visitId are the exceptions — they are defined above so all tests share one consistent value.",

  "headerContext": {
    "_comment_branchOptions": [
      "Available Branch options in the top navigation header:",
      "  - Main Branch",
      "  - Jeddah",
      "  - Branch 3",
      "  - Branch 4",
      "  - Branch 5",
      "  - Branch 6",
      "  - Branch 7"
    ],
    "targetBranch": "Main Branch",

    "_comment_locationOptions": [
      "Available Location/System options in the top navigation header:",
      "  - In Center",
      "  - Home Hemodialysis"
    ],
    "targetLocation": "Home Hemodialysis"
  }
}
